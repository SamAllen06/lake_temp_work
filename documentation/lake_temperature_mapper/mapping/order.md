# Order (order.py)

## Purpose
Acts as a data structure for storing instructions for changing an input
parameter so that the resulting outputs can be mapped.

## Functionality
Stores the name of the order, the parameters it modifies, the number of samples
that will be taken to interpolate the input, the values of the first sample, and
the values of the last sample.
