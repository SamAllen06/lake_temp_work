# Parameter Range (parameter_range.py)

## Purpose
Stores information about the range used by a single parameter inside an order.
An order can have multiple ranges if it modifies multiple parameters.

## Functionality
Stores a start and end value. It is later stored in a map with the parameter 
name as the key and the ParameterRange as the value.
