# No Check Input Verifier (no_check_input_verifier.py)

## Purpose
Serves as an InputVerifier for Field(s) that do not need input validation.

## Functionality
Always accepts the input, returning None when asked to validate it. Formats the
input by simply returning the input.
