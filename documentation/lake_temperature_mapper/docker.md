# Docker
APP/docker/

## Purpose
Contains the actual model we are testing (currently elmtest.exe) along with
configuration files that enable us to test it. See the Dockerfile for exact
details as to what is imported into the docker image.
