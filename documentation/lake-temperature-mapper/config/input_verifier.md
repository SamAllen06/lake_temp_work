# Input Verifier (input_verifier.py)

## Purpose
Verifies user input and formats it into a usable form, which is later stored in
the configuration file.

## Functionality
Verifies user input using rules defined by the subclass, returning an error
message if the input is not valid, and None if it is.

Formats user input using rules defined by the subclass.
