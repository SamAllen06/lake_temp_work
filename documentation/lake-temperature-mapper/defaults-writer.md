# Defaults Writer (defaults_writer.py)

## Purpose
Resets the parameters file to its default values.

## Functionality
Reads in a parameter defaults file (typically lakeparams_defaults.txt) and 
writes those values to the parameters file (typically lakeparams.txt).
