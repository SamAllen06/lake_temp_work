# Group Output Writer (group_output_writer.py)

## Purpose
Groups OutputWriter(s) together, and simply propegates each function call it
recieves through each of them. Allows the program to run with multiple sources
of output, or none at all.
