# Configure Mapper (configure_mapper.py)

## Purpose
Generates a configuration file for the mapper, meaning the user doesn't have
to retype paths every time they run the mapper.

## Functionality
Saves a path to the binary being tested, the range file, the parameters
file, the reference output file, and the test output file. Writes to 
scripts/config/mapper.conf.
