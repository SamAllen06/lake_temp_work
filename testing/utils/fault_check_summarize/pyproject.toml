[project]
name = "fault-check-summarize"
version = "1.0.0"
requires-python = ">=3.12,<4.0"
dependencies = []

[project.scripts]
fcsum = "fcsum:main"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
